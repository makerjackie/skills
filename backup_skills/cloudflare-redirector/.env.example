CLOUDFLARE_API_TOKEN=your_cloudflare_api_token
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id

# Optional: default CNAME target used by dns:sync script.
# If omitted, script falls back to each route's zone_name.
DNS_TARGET_HOST=example.com
